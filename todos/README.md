# TODOs

项目所有 TODO 统一管理在此目录下。每个 TODO 一个独立文件。

## 规则

- 新建 TODO 必须放在 `todos/` 目录下，禁止在项目其他位置创建 TODO 文件
- 文件命名：`<简短描述>.md`，使用英文短横线分隔（如 `agent-ctrl-api.md`）
- 完成的 TODO 将条目标记为 `[x]`，全部完成后将文件移至 `todos/done/` 或删除
